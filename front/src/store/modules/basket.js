export default {
    state: {
        basket: null,
    },
    mutations: {},
    actions: {
        // getBasket({commit}){}
        // loginUser({ commit }, { email, password }) {},
        // logoutUser({ commit }) {},
    },
    getters: {
        // user: state => state.user,
        // checkUser: state => state.user !== null,
    }
};
